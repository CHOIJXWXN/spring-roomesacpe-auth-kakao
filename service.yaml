apiVersion: v1
kind: Service
metadata:
  labels:
    app: jerrie-was
  name: jerrie-was-svc
spec:
  ports:
    - name: 8080-8080
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: jerrie-was
  type: ClusterIP